module Slideable
  def moves(piece)
    possible_dirs = piece.move_dirs
    possible_moves
    if possible_dirs.include?(diagonal)

    end
    if possible_dirs.include?(ver_hor)

    end
  end
end 

module Stepable

end